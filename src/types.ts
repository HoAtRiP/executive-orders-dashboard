export interface ExecutiveOrder {
  eo_number: string;
  title: string;
  president: string;
  signing_date: string;
  publication_date: string;
  citation: string;
  pdf_url?: string;
  html_url?: string;
}
